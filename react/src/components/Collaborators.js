import React, { Component } from 'react';

class Collaborators extends Component {
  constructor(props) {
  super(props);
  }
  render() {
      return (
      );
  }
}

export default Collaborators;
